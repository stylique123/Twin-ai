{
  "$schema": "./brief_consumers.schema.md",
  "$why": [
    "THE SINGLE SOURCE OF TRUTH for who READS each answer the creator gives us.",
    "",
    "A creator answers nine questions during the scan and on confirm. Every answer",
    "is validated, sanitized and written to brand_voices.pre_script_brief. Four of",
    "them are then read by nothing at all: the script generator never learns whether",
    "it is writing for a doctor, a SaaS founder or a hobbyist, and the captured",
    "product it could name is never offered to it.",
    "",
    "That is a worse state than not asking. A question spends the creator's",
    "attention and buys a promise — implicit but unmistakable — that the answer",
    "changes what they get. An answer nothing reads breaks that promise silently,",
    "and silence is why it survived: every individual PR that added a question was",
    "correct, tested, and shipped green.",
    "",
    "So the rule is mechanical rather than remembered: every key in",
    "BRIEF_STORED_KEYS declares its readers HERE, and check_brief_consumers.mjs",
    "fails the build when this file and the repository disagree.",
    "",
    "WHY UNWIRED KEYS ARE DECLARED RATHER THAN TOLERATED. A guard that simply",
    "demanded a reader for every key could not land until every key had one — so it",
    "would land last, after the next asked-and-discarded fact had already been",
    "added, which is precisely the failure it exists to prevent. Instead the debt is",
    "written down: `readBy: []` plus an `unwiredReason` naming what is missing.",
    "",
    "THE EXEMPTION EXPIRES BY ITSELF. The guard verifies both directions — a key",
    "declared unwired must have NO reader in supabase/functions. The moment someone",
    "wires one up, this file is wrong and the build says so. An exemption that",
    "quietly stays true after it stops being true is how the original bug got in."
  ],
  "$fields": [
    "readBy      — paths under supabase/functions that must contain the key.",
    "              Every path must exist AND mention the key; a stale entry fails.",
    "unwiredReason — required when readBy is empty. Says what is missing, not that",
    "              nothing is missing. Delete it in the same PR that adds a reader."
  ],
  "keys": {
    "goal": {
      "readBy": [
        "supabase/functions/generate-blueprint/index.ts"
      ]
    },
    "audience": {
      "readBy": [
        "supabase/functions/generate-blueprint/index.ts"
      ]
    },
    "offer": {
      "readBy": [
        "supabase/functions/generate-blueprint/index.ts"
      ]
    },
    "forbiddenClaims": {
      "readBy": [
        "supabase/functions/generate-blueprint/index.ts"
      ]
    },
    "workKind": {
      "readBy": [
        "supabase/functions/generate-blueprint/index.ts"
      ]
    },
    "workKindOther": {
      "readBy": [
        "supabase/functions/generate-blueprint/index.ts"
      ]
    },
    "promotes": {
      "readBy": [
        "supabase/functions/generate-blueprint/index.ts"
      ]
    },
    "productEvidence": {
      "readBy": [
        "supabase/functions/generate-blueprint/index.ts"
      ]
    },
    "alsoWantsToMake": {
      "readBy": [],
      "unwiredReason": "Asked on confirm and read by nothing. Either give it a consumer or stop asking it."
    },
    "commercialTies": {
      "readBy": [
        "supabase/functions/generate-blueprint/index.ts"
      ],
      "readerNote": "Read for EXACTLY ONE decision: the product-scene refusal. `recordedNoProduct` used to read `ownedEntity.relationship === 'NONE'`, and the entity query filters `.in('relationship', ['OWN_PRODUCT','OWN_SERVICE'])` — so a NONE row can never come back and that branch was dead by construction. The \"I sell nothing\" answer lives here instead, and nothing read it. ⚠️ THIS ENTRY'S OLD unwiredReason WAS RIGHT AND IS WHY THE SCOPE IS THIS NARROW. It warned that wiring this key would add a second interpretation of a fact `brief.promotes` already carries, including the affiliate and sponsor disclosures `promotesLine` states as non-optional. A draft of the wiring PR did exactly that and was cut back: `rel`, the claim permissions and `disclosureRequired` all still read the entity alone. The refusal is the one decision with NO second channel — `readyNothingToSell` gates only the readiness questions, and `promotes === 'none'` is about somebody else's product, not about this creator having none."
    },
    "contentGoals": {
      "readBy": [
        "supabase/functions/generate-blueprint/index.ts"
      ]
    },
    "desiredFormats": {
      "readBy": [
        "supabase/functions/generate-blueprint/index.ts"
      ]
    },
    "audienceKnowledge": {
      "readBy": [
        "supabase/functions/generate-blueprint/index.ts"
      ]
    },
    "formatExploration": {
      "readBy": [
        "supabase/functions/generate-blueprint/index.ts"
      ]
    },
    "ownProductKind": {
      "readBy": [],
      "unwiredReason": "Read by creatorProfileQuestions to branch the product questions and by productEntity when minting the owned entity. No edge reader: the writer learns about a product from the claimed entity, not from this."
    },
    "ownServiceKind": {
      "readBy": [],
      "unwiredReason": "As ownProductKind. It shapes what is asked, not what is written."
    },
    "defaultCta": {
      "readBy": [
        "supabase/functions/generate-blueprint/index.ts"
      ]
    },
    "onCamera": {
      "readBy": [
        "supabase/functions/generate-blueprint/index.ts"
      ]
    },
    "productFacts": {
      "readBy": [
        "supabase/functions/generate-blueprint/index.ts"
      ],
      "readerNote": "⚠️ THIS KEY ESCAPED THIS REGISTRY ENTIRELY UNTIL 0198, AND THAT IS THE HOLE THE GUARD NOW CLOSES. generate-blueprint wrote `stable.productFacts` from the readiness question labelled \"Specific features, numbers or outcomes this video is allowed to state\", but the key was absent from BRIEF_STORED_KEYS — so this guard, which exists precisely to stop an answer nothing reads, could not see it and demanded nothing. It was also absent from 0109's shape CHECK, so the database rejected the persist; the write is ONE update of the whole brief, so the rejection took `offer`, `promotes` and `defaultCta` with it. Measured 2026-09-10: 0 of 52 voices carry productFacts and 0 of 51 carry a stored defaultCta, from that one cause. The reader is a claim line marked as the CREATOR'S OWN STATEMENT, deliberately NOT as a verified fact — `usableProductFacts` earns its grade from a classifier reading an authoritative page; this is a sentence a person typed. It also refuses to licence an outcome: `unionApproved` stays the only thing that may, or a free-text box becomes the back door around the approval."
    },
    "confirmedAudiencePain": {
      "readBy": [],
      "unwiredReason": "Stored durably as of 0195; the writer still reads the UNCONFIRMED inference. generate-blueprint:5853 resolves `vp?.audience_pain ?? dna.pain`, and adding `brief.confirmedAudiencePain` to the front of that chain is one line. What blocks it is the LABEL, not the value: `prov('audiencePain')` comes from projectBrandTruth, whose only sources are the DNA and the synthesis, so the writer would quote the creator's confirmed sentence and still mark it guessed. A wrong provenance is worse than none — §10.2 turns on exactly that distinction. Wiring it means threading the confirmation into brandTruth as a self-reported source, which needs its own evidence and its own PR."
    },
    "confirmedDreamOutcome": {
      "readBy": [],
      "unwiredReason": "The same, for generate-blueprint:5854 (`vp?.dream_outcome ?? dna.dream`). Blocked on the same provenance question and wired in the same change."
    }
  }
}
