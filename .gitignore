node_modules
dist
.expo/
.env
.env.local
.env.*.local
# .env.production is ignored so a future one holding a REAL secret can't slip in.
# apps/web/.env.production (anon key only — public by design) predates this rule
# and stays tracked; update it with `git add -f` when needed.
.env.production
*.log
.DS_Store
.claude/
__pycache__/
*.pyc

# Pilot run state: a frozen sample, un-locked labels, and reviewer identity.
.twinai-pilot/
# Watched-session and labelling data never enters the repo: a creator's verbatim
# words and an operator's un-locked judgments.
.twinai-labels/
.twinai-d1/
*.tsbuildinfo
