{
  "name": "twinai-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "TwinAI monorepo \u2014 responsive web app (Vercel) + shared code on one headless Supabase + VPS backend. The web app is the single client surface (phone + desktop).",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "web": "npm run dev -w @twinai/web",
    "web:build": "npm run build -w @twinai/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "eval": "node eval/run.mjs"
  }
}
